local Class = require("lib.class")

local SpellBase = Class("SpellBase")

function SpellBase:initialize() -- luacheck: ignore
end

function SpellBase:cast(e, world) -- luacheck: ignore
end

function SpellBase:update(e, world, dt) -- luacheck: ignore
end

return SpellBase
